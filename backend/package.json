{
  "name": "queuectl",
  "version": "1.0.0",
  "description": "Production-quality CLI job queue manager backed by SQLite",
  "type": "module",
  "bin": {
    "queuectl": "./src/index.js"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "commander": "^13.1.0",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "jest": "^30.4.2"
  }
}
